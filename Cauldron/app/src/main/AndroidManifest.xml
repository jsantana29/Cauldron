<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fiu.team5cen4010.cauldron">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/cauldron_icon_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/cauldron_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TitleActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Adapter.RecipeView" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".ForgotPassword"></activity>
    </application>

</manifest>